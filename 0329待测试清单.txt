修改回顾：

参考音频变更检测与重置功能（audio_panel.py）

添加了 self.last_ref_audio_path 记录上次使用的参考音频路径。

添加了 reset_all_segments() 方法，用于清空所有片段状态、重录队列、删除参考音频缓存。

修改了 start_generation()：检测参考音频是否变更，若变更则提示并重置全部片段；否则检查是否全部已生成，根据情况提示继续生成或全部重新生成。

添加了 _are_all_segments_generated() 和 _update_start_button_text() 方法，动态更新按钮文字（“开始生成音频”/“继续生成音频”）。

修改了 start_generation_thread() 和 _generate_original_segments()，支持只生成指定的片段列表（target_segments），实现“继续生成”功能。

在多个位置调用 _update_start_button_text() 以保持按钮状态同步。

AI生成参考音频后自动调用 Whisper 并缓存（audio_panel.py 的 ai_generate_audio 中的 confirm 函数）

将原来的同步 extract_reference_text 改为后台线程执行，避免 UI 卡死。

确认后立即上传并提取文本，更新 ref_audio_filename、ref_text 并保存缓存。

字幕生成自动重试（aliyun_file_trans.py）

为 generate_subtitle 增加了重试机制（默认最多2次），失败后等待并重试。

测试清单：

一、基础功能测试（确保原有流程未破坏）
新建项目 → 润色段落 → 上传本地参考音频 → 开始生成音频

预期：正常生成所有片段，进度条更新，生成完成后按钮变为“开始生成音频”，所有片段可播放、重录、确认。

检查：无报错，所有片段音频文件生成在 segment_xxx.mp3，最终合成正常。

使用AI生成参考音频 → 确认 → 开始生成音频

预期：AI生成窗口关闭后，参考音频路径显示正确；点击“开始生成音频”时，后台自动调用 Whisper 提取文本，然后正常生成所有片段。

检查：reference_cache.json 被正确写入；生成过程中无 UI 卡死。

生成过程中点击“停止生成音频”

预期：停止生成，按钮变为“继续生成音频”（如果有未完成片段），再次点击继续生成剩余片段。

检查：停止后不生成新片段，继续后只生成未完成的片段。

全部片段生成后，再次点击“开始生成音频”

预期：弹出提示“所有音频片段均已生成...”，询问是否全部重新生成。确认后重置所有片段并重新生成全部；取消则无操作。

检查：重置后所有片段状态为“待生成”，原有音频文件被忽略，重新生成后新文件覆盖。

生成部分片段后，更换参考音频（上传新音频或AI生成新音频） → 点击“开始生成音频”

预期：弹出提示“参考音频已更换...”，确认后重置所有片段并全部重新生成；取消则无操作。

检查：重置后使用新参考音频生成全部片段，旧缓存被删除。

字幕生成测试（针对阿里云）

模拟网络故障或第一次失败：应自动重试，最终成功生成 input.srt，用户无需手动点两次。

连续失败两次后应正确回调失败。

二、边界情况与异常测试
没有任何分段时点击“开始生成音频” → 提示“请先生成分段”。

没有参考音频时点击“开始生成音频” → 提示“请先选择或生成参考音频”。

生成过程中程序被强制关闭 → 重新打开项目后，已生成的片段应自动关联（load_segments_from_labeled_text），状态为“已生成”，未生成的为“待生成”。按钮应为“继续生成音频”。

重录功能：更换参考音频后，旧的重录队列应被清空；新参考音频下重录应正常工作。

三、按钮文字状态检查
无任何片段生成时，按钮文字为“开始生成音频”。

部分片段生成后，按钮文字变为“继续生成音频”。

全部片段生成后，按钮文字恢复为“开始生成音频”。

生成过程中按钮禁用，停止后恢复。

四、回归测试（确保未影响其他模块）
视频提示词生成（auto_align_duration）仍能正常工作。

最终音频合成（combine_audio）仍能正常工作。

字幕生成功能（阿里云）仍能正常工作（包括自动重试）。

历史项目加载后，按钮文字和片段状态正确。