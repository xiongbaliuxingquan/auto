# utils/extract_persona_scene.py
"""
从故事中提取人物和场景设定，返回格式化的纯文本。
"""
from .ai_utils import call_deepseek

def extract_from_story(story_text):
    prompt = f"""
请根据以下故事，提取所有主要人物和主要场景的详细视觉特征。以纯文本格式输出，要求结构清晰，易于阅读。

故事：
{story_text}

输出格式要求：
- 首先写“【人物设定】”，然后每个主要人物一段，用“人物名称：”开头，下面用“- ”列出特征，例如：
  【人物设定】
  李老师：
    - 年龄：约60-65岁
    - 体型：中等偏瘦，略显单薄
    - 发型：灰白短发，整齐利落
    - 面容：面容清瘦，眼角有细纹，眼神深邃
    - 日常衣着：深灰色羊绒衫、素色丝巾（后期棉麻衬衫）
    - 习惯动作：手指习惯性摩挲物品，独处时动作缓慢
  （每个人物都这样列出）

- 然后写“【场景设定】”，每个主要场景一段，用“场景名称：”开头，下面用“- ”列出特征，例如：
  【场景设定】
  场景一：清晨·大平层餐厅：
    - 类型：室内，现代简约风格豪宅餐厅
    - 家具：一张极长的实木餐桌，配有十二把高背餐椅
    - 关键道具：一碗粥、几碟小菜、十二把空椅子
    - 环境特点：巨大的落地窗，窗外城市天际线
  （每个场景都这样列出）

不要输出任何额外内容，只输出上述两部分。
"""
    result = call_deepseek(prompt, temperature=0.3, max_tokens=2000)
    # 清理可能的 markdown 代码块
    result = result.strip()
    if result.startswith("```"):
        result = re.sub(r'```\w*\n?', '', result)
    return result  # 直接返回纯文本